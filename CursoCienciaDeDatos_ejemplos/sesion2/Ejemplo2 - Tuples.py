# Las Tuples son con parentesis y separados los valores por comas
# Nos permite modificar 
thistuple = ("apple", "banana", "cherry")
print(thistuple)

#Podemos acceder por medio de indices
print(thistuple[0])
print(thistuple[2]) 

#thistuple[0] = "orange" Python no permite modificar los valores de una Tuple
print(type(thistuple))